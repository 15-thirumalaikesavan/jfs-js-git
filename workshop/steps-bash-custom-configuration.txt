BASH CONFIGURATION
1.CREATE .bashrc. inside the user directory (C:\Users\Subbu)
 
2.CONFIGURE ALIAS
alias gs='git status'
alias gpu='git pull'
alias ngs='ng serve -o'
alias ngsp='ng serve -o --port 4201'
alias ni='npm install -save "$1"'
alias nin='npm init'

function gc() {
    git add .
    git commit -a -m "$1"
}
function gp() {
    git add .
    git commit -a -m "$1"
    git push
}
# function gp() {
#     git pull
# }
# function ngs(){
# ng serve -o
# }

3. CONFIGURE VS CODE TO USE BASH
ctrl+shift+p Terminal: Select Default Shell : Git Bash

4. OPEN A NEW BASH
test by typing gs
